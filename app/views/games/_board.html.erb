<%= content_tag(:div, "", data: {
    controller: "chess",
    game_id: game.id,
    player_id: current_user.id,
    fen: game.fen,
    turn: game.turn[0],
    orientation: game.orientation(current_user)
}, style: "width: 500px") do %>
  <div id="board"></div>

<% end %>
